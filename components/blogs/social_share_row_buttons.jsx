/** @format */

import React from 'react';

const SocialShareRowButtons = ({ className, url, socialMedia }) => {
	return <div className={`${className} d-flex my-auto flex-wrap`}>social icons here dera</div>;
};

export default SocialShareRowButtons;
