/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['dbis5km5hrwi4.cloudfront.net', 'd2nq068qy4r71u.cloudfront.net', 'res.cloudinary.com'],
	},
};

module.exports = nextConfig;
